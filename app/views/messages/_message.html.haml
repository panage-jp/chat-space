.post{data: {id: message.id}}
  %p.post__user
    = message.user.name
    %span.post__date 
      = message.created_at.strftime("%Y/%m/%d %H:%M")
  .post__content 
    -if message.body.present?
      = simple_format(message.body)
    = image_tag message.image.url if message.image.present?